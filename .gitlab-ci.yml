stages:
  - lint
  - test

lint:
  image: python:3.12
  script:
    - pip install ruff
    - ruff check .

test:
  stage: test
  image: python:3.12
  script:
    - pip install anthropic mcp netmiko jinja2 requests pytest
    - python -m pytest tests/ -v
